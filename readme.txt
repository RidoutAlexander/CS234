In order to compile the programs, put them in a location on your computer that you would like to access them from. Open the terminal 
and type cd followed by the next level in the directory of the .java file. Once you are inside the directory folder,
type javac nameoffile.java to compile the program and then type java nameoffile.java to run the program in the terminal.